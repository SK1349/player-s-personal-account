spring:
  datasource:
    url: jdbc:postgresql://service-db:5432/CS
    username: postgres
    password: Sofa44@))cjy
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:

logging:
  level:
    org.flywaydb: DEBUG

server:
  port: 8084